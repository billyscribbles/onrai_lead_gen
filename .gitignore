.env
.venv/
output/*
# ...but track the seed CSV so the dashboard ships with leads (ingested on first boot).
!output/melbourne_no_website_leads.csv
__pycache__/
*.pyc
.pytest_cache/
.pnp.*
.yarn/
node_modules/

# ABN bulk extract (large, ~1GB+ — download locally, never commit)
data/

.DS_Store
web/dist/

# Superpowers SDD scratch (ledger, briefs, reports, diffs)
.superpowers/
